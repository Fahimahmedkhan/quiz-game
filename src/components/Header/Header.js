import React from 'react';
import Banner from '../Banner/Banner';
import Nav from '../Nav/Nav';

const Header = () => {
    return (
        <div>
            <Nav></Nav>
            <Banner></Banner>
        </div>
    );
};

export default Header;